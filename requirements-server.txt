# ClineGate server dependencies (headless Linux/VPS deployment).
# Same runtime as requirements.txt minus the desktop shell (pywebview,
# pythonnet) and icon tooling (Pillow).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
httpx>=0.27,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
PyYAML>=6.0,<7.0
sse-starlette>=2.1,<3.0
python-multipart>=0.0.12,<1.0
